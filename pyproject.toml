[project]
name = "stow-dotfiles"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hyprtheme",
]

[tool.uv.workspace]
members = [
    "hyprtheme",
]

[tool.uv.sources]
hyprtheme = { workspace = true, editable = true }
